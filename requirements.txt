# Runtime dependencies for the Agentic Research Assistant.
# For development extras (pytest, ruff) install with: pip install -e ".[dev]"
#
# Pinned to exact versions tested in CI, for a reproducible Docker build.
# pyproject.toml keeps looser '>=' ranges for library-style installs; this
# file is what the Dockerfile actually installs from. To upgrade, bump a
# version here, run `pytest`, and update both files together.
# Note: this pins only the direct/top-level dependencies, not their full
# transitive closure -- for a true lockfile, generate one with pip-tools
# (`pip-compile`) or uv.

anthropic==0.115.0
langgraph==1.2.7
faiss-cpu==1.14.3
sentence-transformers==5.6.0
pypdf==6.14.2
arxiv==4.0.0
pydantic==2.13.4
pydantic-settings==2.14.2
numpy==2.5.0
tenacity==9.1.4
rich==15.0.0
fastapi==0.139.0
uvicorn[standard]==0.49.0
prometheus-client==0.25.0
