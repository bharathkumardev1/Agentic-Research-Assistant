# Leave VITE_API_URL unset to run entirely on the built-in offline demo data.
# Point it at a deployed backend to get real answers from the pipeline.
VITE_API_URL=https://agentic-research-assistant-xzno.onrender.com

# Only needed if the backend has WEB_API_KEY set.
VITE_API_KEY=
